[project]
name = "fastcache"
version = "0.1.0"
description = "High-performance in-memory caching library with LRU/LFU eviction"
requires-python = ">=3.8"
license = {text = "MIT"}

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends._legacy:_Backend"
